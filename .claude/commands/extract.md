---
description: "[Pipeline] Map a raw transcript to a module template schema and write the structured draft"
---
Read `.agents/skills/extract/SKILL.md` and follow its instructions precisely.

Arguments (module_id and --workspace path): $ARGUMENTS
