## Goal: Make "Closing the Loop: Red-Teaming & Refining a DDO Document (v0.0.2)" accurate for v0.0.3

### Tutorial
Path: tutorials/ddo-adversarial-loop-v0.0.2/tutorial.md

### What I'm trying to accomplish
Reflect all v0.0.3 breaking and deprecation changes — new patch ops, deprecated op migration paths, DanglingRefError, NC-13 path whitelist, and per-op field validation — while preserving the v0.0.2 conceptual walkthrough and section structure intact.

### Tutorial overview
This tutorial walks the adversarial loop end-to-end on the Biodegradable Polyester Optimization Report: Red Team critique by the `scientific_reviewer` persona, batched Interview Q&A resolving findings, and safe YAML patching via the Refine pipeline — closing the one-way v0.0.1 render pipeline into a critique-resolve-refine cycle.

### Sections that need attention
All sections are flagged for a full accuracy pass; the following have confirmed v0.0.3 drift:

1. **Phase 2 — Interview** — ops table and examples use deprecated ops; new ops undocumented
2. **Phase 3 — Refine** — DanglingRefError, NC-13 path whitelist, sequential-index shift, pre-validation not mentioned
3. **Troubleshooting** — one row has a stale "deferred to v0.0.3" note; missing rows for new error types
4. **Overview / Prerequisites / Phases 1, 5 / Expected Output / Related** — full accuracy pass requested

### Known failures

**Phase 2 — Interview: deprecated `append_evidence` op in ops table**
- Location: Phase 2, decision table column "Patch op"
- Current: `add_evidence` decision maps to `append_evidence` (listed as current op)
- v0.0.3: `append_evidence` is **deprecated** (still runs for compat; removed in v0.0.4)
- Fix: Mark `append_evidence` as deprecated in the table; show migration: `{op: append, target: "evidence_bank", value: {...}}`

**Phase 2 — Interview: deprecated `append_evidence` in F-004 YAML example**
- Location: Phase 2, `resolutions:` YAML block, F-004 entry
- Current:
  ```yaml
  patch:
    op: append_evidence
    value:
      id: gc_monomer_residue
      ...
  ```
- v0.0.3: Should use `{op: append, target: "evidence_bank", value: {...}}`
- Fix: Update the F-004 patch block in the example to the new form; add `target: evidence_bank` field

**Phase 2 — Interview: `append_review_log` implied but deprecated**
- Location: Phase 2, decision table — `acknowledge` decision row says `null → refine routes to meta.review_log`; the internal op was `append_review_log`
- v0.0.3: `append_review_log` is **deprecated**; preferred form is `{op: append, target: "meta.review_log", value: {...}}`
- Fix: Add a note that acknowledge patches may explicitly use `{op: append, target: "meta.review_log"}` rather than relying on the deprecated internal routing

**Phase 2 — Interview: new ops `append`, `delete`, `insert` not documented**
- Location: Phase 2, decision table and surrounding prose
- v0.0.3 added three structural ops not mentioned anywhere in the tutorial:

  | Op | Required fields | Key constraint |
  |----|----------------|----------------|
  | `append` | `target` (list path, no `[N]` suffix), `value` | Never auto-vivifies a missing list |
  | `delete` | `target` (must end in `[N]`); NO `value` field | Triggers `DanglingRefError` if evidence_bank entry still referenced |
  | `insert` | `target` (list path, no `[N]` suffix), `at` (non-neg int, not bool/float), `value` | `at > len(list)` is hard error |

- Fix: Extend the ops table or add a subsection covering all six current ops; distinguish deprecated from current

**Phase 2 — Interview: `validate_interview_log()` per-op enforcement not mentioned**
- Location: Phase 2, commit step prose
- v0.0.3: `validate_interview_log()` (in `ddo/review.py`) now enforces per-op field rules **before** `apply_patches` is called — malformed logs (e.g. `delete` with a `value` field, `insert` without `at`) are caught at validation, not during patching
- Fix: Add a sentence in Phase 2 or Phase 3 noting that `validate_interview_log()` validates op fields before refine runs

**Phase 3 — Refine: `DanglingRefError` not documented**
- Location: Phase 3, Step 3 (apply patches) prose
- v0.0.3: `_dangling_ref_check()` runs before any `delete evidence_bank[N]` patch; if the deleted entry's `id` is still referenced in `content.sections[*].evidence[]`, it raises `DanglingRefError` with `.paths` listing every referencing location
- Behavior: the entire patch batch aborts; `document_data.yaml` is byte-identical
- Resolution pattern: issue `set` patches on all referencing sections first (remove the ID from their evidence lists), then re-run with the `delete` patch
- Fix: Add a DanglingRefError subsection or Troubleshooting row

**Phase 3 — Refine: NC-13 path whitelist not documented**
- Location: Phase 3, Step 3 prose (mentions `parse_path`, "never `eval`")
- v0.0.3: `parse_path()` now enforces: key segments must match `[a-zA-Z_][a-zA-Z0-9_]*`; bracket indices must be `\d+` only — `[-1]`, `[*]`, `[0x1f]` all raise `ValueError`
- The tutorial's existing examples all use well-formed paths, so no examples break
- Fix: Add one sentence noting the character-class constraint; add a Troubleshooting row for the `ValueError` case

**Phase 3 — Refine: sequential-index shift warning missing**
- Location: Phase 3, Step 3 or a new warning box
- v0.0.3: When multiple index-bearing ops (`delete`, `insert`) target the same list within a single interview log, indices shift as patches apply in order. A `delete [2]` followed by another `delete [2]` removes what was originally index 3.
- Fix: Add a warning (callout box or bold note) before the refine phase's patch application step

**Troubleshooting: stale "deferred to v0.0.3" note**
- Location: Troubleshooting table, row: `set target ... is not a leaf scalar / would change type`
- Current: "Use a leaf-scalar `set`; record structural reshapes as `acknowledge`/`defer` **(deferred to v0.0.3)**."
- v0.0.3 is now the current version; structural ops are available
- Fix: Remove the parenthetical; replace with: "Use a leaf-scalar `set` for text edits; for structural changes use `append`, `insert`, or `delete` (available in v0.0.3+)."

**Troubleshooting: missing rows for new error types**
- Missing entries to add:

  | Symptom | Cause | Fix |
  |---------|-------|-----|
  | `DanglingRefError: evidence_bank[N] still referenced at [...]` | `delete evidence_bank[N]` while the entry's ID is still in a section's evidence list | Issue `set` patches to remove the ID from referencing sections first, then retry the delete |
  | `ValueError: invalid path segment '...'` | Path key contains characters outside `[a-zA-Z_][a-zA-Z0-9_]*`, or index is not a plain integer | Use only alphanumeric/underscore key names and non-negative plain integers in paths |
  | `ReportValidationError: insert requires 'at' field` | `insert` op missing the `at` field | Add `at: <non-negative integer>` to the patch |
  | `ReportValidationError: delete forbids 'value' field` | `delete` op includes a `value` key | Remove the `value` field from delete patches |

### Environment
- uv: 0.9.15
- Python: 3.10.12
- Test suite: 188 tests (collected and passing as of 2026-06-30)

### What the fix agent MAY change
- Tutorial text (fix deprecated ops, add new ops documentation, fix stale troubleshooting rows)
- Tutorial input/output example files in `tutorials/ddo-adversarial-loop-v0.0.2/input_files/` and `output_files/` (update YAML examples to v0.0.3 op syntax)

### What the fix agent must NOT change
- Source code (`ddo/*.py`, `tests/`, etc.)
- Pinned dependencies (`pyproject.toml`, `uv.lock`, `ddo/build.py.lock`)
- Tutorial section structure, ordering, or learning objectives

### Instructions for the fix agent

Work through `tutorials/ddo-adversarial-loop-v0.0.2/tutorial.md` section by section:

1. **Full accuracy pass first** — read Overview, Prerequisites, Phases 1 and 5, Expected Output, and Related for any other version-specific drift before touching the known failures
2. **Phase 2 — Interview ops table** — update the decision table: mark `append_evidence` and `append_review_log` as deprecated with migration forms; add rows or a subsection for `append`, `delete`, and `insert` with their required/forbidden fields
3. **Phase 2 — F-004 YAML example** — update the patch block from `op: append_evidence` to `{op: append, target: "evidence_bank", value: {...}}`; verify the updated YAML is syntactically valid
4. **Phase 2 — pre-validation note** — add one sentence noting that `validate_interview_log()` enforces per-op field rules before `apply_patches` runs
5. **Phase 3 — DanglingRefError** — add a callout or subsection documenting the error, the `.paths` output format, and the resolution pattern (clear referencing sections first, then delete)
6. **Phase 3 — NC-13 path whitelist** — add one sentence to the `parse_path` prose; add a Troubleshooting row
7. **Phase 3 — sequential-index shift warning** — add a warning before or within the patch application step
8. **Troubleshooting table** — fix the stale "deferred to v0.0.3" row; add the four new rows listed in Known Failures
9. **Input/output example files** — update `output_files/interview_log_v1.yaml` F-004 patch block to match v0.0.3 syntax; verify any other YAML examples in the directory
10. **Verify** — run `uv run pytest -q` to confirm all 188 tests still pass; scan all sections top-to-bottom for any remaining v0.0.2-only references

### Constraints
- Do not change the tutorial's learning objectives, section structure, or section order
- Additions should augment the v0.0.2 walkthrough, not replace it — v0.0.3 content is additive
- Prefer the smallest change that makes content accurate; no unnecessary rewrites of correct prose
- Keep all 188 project tests passing (`uv run pytest -q` must exit 0)
- Do not touch source code or pinned dependencies
