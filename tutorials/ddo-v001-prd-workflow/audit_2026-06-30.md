## Goal: Make "DDO v0.0.1: From Topic to Document — PRD YAML Workflow" accurate for v0.0.3

### Tutorial
Path: tutorials/ddo-v001-prd-workflow/tutorial.md

### What I'm trying to accomplish
Update the tutorial so every instruction, version reference, and status claim is accurate against the current v0.0.3 codebase. The tutorial's scope (v0.0.1 ingest → render pipeline) should not expand, but stale counts, roadmap language, and any other drifted details must be corrected.

### Tutorial overview
This tutorial walks through the full DDO v0.0.1 document-generation pipeline using a PRD as the example artifact: start from a structured YAML source file, validate it against the DDO contract, and render to PDF, HTML, and Markdown via a single deterministic command.

### Sections that need attention
All sections are flagged for review; the following have confirmed drift:

1. **Prerequisites** — stale test count
2. **Understanding the PRD Schema** — review sentinel token and contract rules for accuracy
3. **Steps 1–3 / Expected Output / Troubleshooting** — full accuracy pass
4. **Related** — stale roadmap language; adversarial loop shipped

### Known failures

**Prerequisites — stale test count**
- Location: `## Prerequisites`, code block `uv run pytest -q`
- Current text: `# Expected: 78 passed`
- Actual (v0.0.3): `188 tests collected / passed`
- Fix: Change the comment to `# Expected: 188 passed`

**Related — adversarial loop described as roadmap**
- Location: `## Related`, last bullet
- Current text: `"DDO adversarial review loop (v0.0.2 roadmap)"` — describes the skills as "out of scope for v0.0.1" and future-tense ("Once available, they will…")
- Actual: v0.0.2 shipped 2026-06-29 and is fully available; `tutorials/ddo-adversarial-loop-v0.0.2/` exists
- Fix: Remove "roadmap" language and update the bullet to read as a shipped feature, with a direct pointer to `tutorials/ddo-adversarial-loop-v0.0.2/tutorial.md`

### Environment
- uv: 0.9.15
- Python: 3.10.12
- Test suite: 188 tests (collected and passing as of 2026-06-30)

### What the fix agent MAY change
- Tutorial text (fix outdated counts, version references, stale roadmap language)
- Tutorial input/output files in `tutorials/ddo-v001-prd-workflow/input_files/` and `output_files/` if any content there is stale

### What the fix agent must NOT change
- Source code (`ddo/*.py`, `tests/`, etc.)
- Pinned dependencies (`pyproject.toml`, `uv.lock`, `ddo/build.py.lock`)
- Tutorial section structure, ordering, or learning objectives

### Instructions for the fix agent

Work through `tutorials/ddo-v001-prd-workflow/tutorial.md`, focusing on the sections listed above:

1. **Fix in order** — address each failing section before moving to the next
2. **Prerequisites** — update `# Expected: 78 passed` to `# Expected: 188 passed`; verify the surrounding prose is still accurate (package names, install instructions)
3. **Understanding the PRD Schema** — verify the sentinel token format `[[DDO::REQUIRES_INPUT:` is the correct gate token (check `ddo/validation.py`); confirm the two invariants (dangling refs, duplicate IDs) are still the only enforced rules; confirm date format `YYYY.MM.DD` is still strict
4. **Steps 1–3 / Expected Output / Troubleshooting** — verify every command, path, flag, and error message against the current `ddo/build.py` and `ddo/validation.py`; check that `tests/integration/test_render_determinism.py` still exists as referenced; verify output file sizes are still approximately correct or update to "approximate" language
5. **Related** — rewrite the final bullet: remove "v0.0.2 roadmap" framing; state that the adversarial loop shipped in v0.0.2 and is available now; add a direct link to `tutorials/ddo-adversarial-loop-v0.0.2/tutorial.md`
6. **Tutorial text fixes** — if any instruction is outdated (wrong path, renamed API, removed option), update the tutorial text to match current reality
7. **Verify** — after changes, run `uv run pytest -q` to confirm all 188 tests still pass; optionally run the smoke-render command from Step 2 against `input_files/prd_example.yaml`
8. **Final check** — scan all sections top-to-bottom for any remaining version-specific references that may have drifted

### Constraints
- Do not change the tutorial's learning objectives, section structure, or section order
- Prefer the smallest change that makes the content accurate
- Keep all 188 project tests passing (`uv run pytest -q` must exit 0)
- Do not touch source code or pinned dependencies
